
//percorre o array 
let times = ['spfc','santos','palmeiras']

times.forEach(function (nome,indice ){ console.log(`${indice+1}) ${nome}`)}) // no foreach passa 3 parametro para funcao, nome do dados que está no objeto, indice , proprio array
times.forEach(nome=> console.log(`) ${nome}`)) // funcao arraw  com um unico parametro 


